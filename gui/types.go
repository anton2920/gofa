package gui

import "github.com/anton2920/gofa/gui/gr"

/* NOTE(anton2920): I defined this separately, because if I ever decide to get rid of 'gr', I will still need these types. */
type (
	Font = gr.Font
	Rect = gr.Rect
)
