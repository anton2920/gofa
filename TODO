Action items:

- Get rid of _syscall.
- Get rid of _alloc.
- Get rid of _buffer.
- Get rid of errors_/panic.go.
- Split gui to pure and abnormal packages.
- Get rid of libx11 depencency.
- Rethink policy on allowing 'init' function in packages.
- Rework container/bplus package:
	- Remove Pager interface — move to just using memory.
	- Complete B+tree implementation.
	- Enhance B+tree algorithms.
- Rework jail container.
- Add ability to allocate file-backed virtual memory to os package.
- Split l10n to pure and abnormal packages.
- Remove usage of standard 'fmt' package from everywhere.
- Remove net/html package.
- Add net/net_.Listener?

New structure:

- containers
	- bplus - my implementation of B+tree, stripped of parametric polymorphism.
	- bheap - bheap implementation from 'You're doing it wrong!'.

- cpu
	- intel - each function represents the instruction with the same name.
	- arm

- database
	- kv (and, most likely, "kvutil") - my implementation of KV-database.
	- wal (and, most likely, "walutil") - my implementation of concurrent WAL.

- gls? (goroutine local storage)

- mem

- net - only protocol implementations.
	- tcp
	- udp
	- tls
	- http - only parser and generator.
	- httputil - convenience functions and "net/http" adapter.

- netutil - convenience functions to listen/connect.

- gpu?

- os - platform layer.
	- posix - POSIX system calls.
		- bsd - for kqueue.
		- linux - for io_uring.
	- windows - WinAPI calls.
	- plan9?

General rules:

- Only "os" package and its subpackages are allowed to allocate memory and interface with operating system.
- Only abnormal packages (i.e. names of which end with '_') are allowed to import "os" and other abnormal packages. The only exceptions to that rule are 'debug_' and 'trace_' packages (because they are doing something only when certain '-tags' are enabled).

- The universal unit of communication is '[]byte'.
- All functions operate on a fixed chunk of memory.
- If memory is insufficient, they 'return mem.ErrNotEnough{Required: <amount>}'.

- Functions in abnormal packages can wrap corresponding functions in regular packages and automatically provide more memory on error.
- Those functions could be automatically generated through 'gpp' (by marking with '//gpp:generate: util(mem)')?

New:

- debug:
	- 'debug.Assert' (C-style) and 'debug.Check' (Alef-style).

- fmt:
	- New type 'fmt.Formatter = { []byte, int }'.
		- W(int), Prec(int)
		- S(string), I(int), String() string, Bytes() []byte
	- s := f.Reset().W(5).S("N = ").D(n).S(", K = ").I(k).S(", results = ").Is(results).String()
	- fmt.Begin().(...).End()?

- log:
	- New type 'Formatter = { fmt.Formatter }'

- mem
	- All allocators from gingerBill's website.

- std/runtime:
	- Variable to panic on calls to 'mallocgc'.

- os
	- OpenFile, OpenEventQueue
	- Read, Write, Close
	- ReadAt, WriteAt
	- ReserveMemory
	- AllocateMemory
	- Listen, Connect
	- ReadEvents

- trace:
	'RDPMC" capabilities with '-tags gofaptrace' or '-tags gofatracepmc'.

Windows to POSIX flags:
- OpenExisting - 0
- OpenAlways - O_CREAT
- TruncateExisting - O_TRUNC
- CreateNew - O_CREAT | O_EXCL
- CreateAlways - O_CREAT | O_TRUNC
